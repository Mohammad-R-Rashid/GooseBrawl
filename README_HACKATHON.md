# GOOSED. — Hackathon Build

An iPhone AR chase game built with Unity 6 (6000.6), URP, AR Foundation 6.6 and the ARKit XR Plug-in.
You scan your floor, place a goose nest, steal the egg, and a ridiculous goose chases you through
your real room. The goose lives in AR world space: it is behind you, you hear it and feel it, and if
you turn the phone around it is right there, casting a shadow on your floor.

Everything is one scene, one game mode, no accounts. The only persistent data is the best score / best time, games
played, your name for the Goose Board and the sound / haptics toggles in `PlayerPrefs`. The only server is the goose's
brain (a Cloudflare Worker), which also hosts the booth leaderboard; the game plays fully offline without it.

(The code, the scene and the bundle id still say Goose Brawl / eggsnatcher on purpose: renaming them would wipe
saved best times on installed phones. Only the user-visible name changed.)

## Project layout

```
Assets/Art/Goose/                 Goose FBX (mesh + skeleton + 61 clips) and textures
Assets/Art/Generated/             Textures generated by Setup (radial, dot grid, soft ring, twig strip + normal, feather, goose occlusion); AppIcon.png is the
                                  honking goose rendered from the model by the `icon` command (GooseIconShot)
Assets/Plugins/iOS/GooseHaptics.mm  Native Core Haptics bridge (UIKit generators as fallback)
Assets/Prefabs/Goose.prefab       Generated by Setup Project
Assets/Prefabs/ARPlaneVisual.prefab, AREnvironmentMesh.prefab
Assets/Scenes/GooseBrawl.unity    The one and only scene (generated by Setup Project)
Assets/Settings/GoosedPostFX.asset  Post-processing profile (generated by Setup; every override is active on purpose, see below)
Assets/Shaders/                   ShadowCatcher (real shadows on the real floor), ContactShadow (multiplicative contact patch)
Assets/Scripts/Core/              GooseGameManager (state machine), ScoreManager, PlayerTracker, HapticsService, GameInput, GooseLog,
                                  GoosePersona (name + grudge), GooseLines (offline script), GooseBrainClient (Cloudflare brain), YellDetector (mic),
                                  PerfProbe (frame-level profiler -> Sentry), PerfBenchmark (render-config matrix), GooseTelemetry (Sentry wrapper), GooseSentryOptions
Assets/Scripts/AR/                ARBootstrapper, ARSurfaceScanner, ARPlacementController, AREnvironmentMeshController, ARLightRig,
                                  AREnvDepthPublisher, MockARController
Assets/Scripts/Goose/             GooseChaseController, GooseMovement, GooseObstacleAvoidance, GooseAttackController (lunge + flap-dash),
                                  GooseVisualController, GooseAnimationResolver, GooseProceduralAnimation, GoosePlaceholderFactory, GooseVoice (the goose talks)
Assets/Scripts/Gameplay/          NestController, EggController, BreadController, DangerFeedbackController, CinematicLookController, ShadowCatcher, ProceduralAssets
Assets/Scripts/Audio/             AudioManager, ChaosAudioController, ProceduralAudio (runtime generated clips, 44.1 kHz)
Assets/Scripts/UI/                GameUIController, UIFactory, GooseLocator, SafeAreaFitter
Assets/Scripts/Editor/            GooseBrawlSetup (menu + auto setup + Configure Sentry), EditorSceneSetup, GoosePrefabBuilder, GooseBrawlMaterials, GooseBrawlValidator,
                                  GooseBrawlRemote, GooseBrawlPostBuild (Info.plist display name "GOOSED.", CoreHaptics link), GooseVoiceExport
Assets/Resources/GooseVoice/      Offline voice bank (m/<beat>_<n>.wav and f/<beat>_<n>.wav, generated by backend/goose-brain/scripts/pregen.ts)
Assets/Resources/GooseBrainUrl.txt  The deployed goose-brain Worker URL (one line)
Assets/Resources/Sentry/          Sentry SDK options (written by Goose Brawl > Configure Sentry from ./sentry.dsn)
backend/goose-brain/              The goose's brain: Cloudflare Agent (Durable Object) + OpenAI + ElevenLabs + R2 + Sentry (see its README)
```

## Design rules (do not change without asking)

- **No background music.** Ever. Only diegetic sound: the goose, your heartbeat and breathing, the crack, the flock, foley.
- Survival time is the score; no points.
- Portrait, one-handed. The goose is a real AR object, never a UI overlay.
- Graphics must read as mature and immersive: real shadows, post-processing, room-matched light, real reflections.
  No decals, no red screen paint, no primitives-in-a-trench-coat props.
- The goose's real speed stays gentle (1.1 -> 1.6 m/s). Urgency comes from flap-dashes, animation speed, honk cadence and feathers.
- **Nothing gameplay-relevant is random.** The demo is one run: escalation is time-based, the grey goose appears every third game,
  fallback lines are picked by round, bread and shouts have fixed costs. The cloud only supplies words and voice, always with an offline fallback.

## Game flow (state machine)

`Boot -> ScanEnvironment -> PlaceNest -> EggReady -> EggStolen -> Chasing <-> Danger -> JumpAttack -> GameOver -> (Run again) EggReady`

The EggStolen beat: you grab the egg ("BREAKFAST TIME!"), it flies into your hand ("GOT IT. LET'S GO COOK."), then it slips,
drops in slow motion (the picture desaturates) and cracks on the floor in front of you: jagged shell pieces, yolk and a white
that spreads out. The crack is what the goose hears. It flies in low **along your current line of sight** with a continuous
wing-beat, lands 2.4-3.2 m in front of you and **glares**: it does not move until it has been in view for half a second (or
4.5 s pass). Then the HUD says **TURN AROUND AND RUN!** and there is a 2 s grace period. On repeat rounds the steal beat plays
faster and a tap skips ahead. The classic spawn-behind-you entrance is still available by turning off `GooseGameManager.flyInEntrance`.

### The goose has a voice, a name and a grudge

The goose talks (ElevenLabs voice, lines written by OpenAI, memory on a Cloudflare Agent; see "Sponsor integrations"). Every
spoken beat is deterministic and every beat has an offline fallback in `Assets/Resources/GooseVoice`, so the demo never waits
on wifi. Shipped setting: the voice is the local bank only (`GooseBrainClient.fetchVoiceAudio` off): nothing is generated or
fetched live in a real-time game; the brain names the goose, keeps the memory and hears the shouts, and every beat plays its
bank line on the beat (`payload.spoken` tells the brain what was said). Coherence rule: the subtitle pill only ever shows
what the voice is saying. Speech has its own source; honk bursts are cancelled and blocked while it plays. Only the newest pending line is retained, and interruptions stop both active and waiting speech. A reaction that has waited more than two seconds behind another
line, or that is still queued when the round ends, is dropped instead of played late. With brain audio switched on, the
shout reply waits 3.5 s and the round-end line 2.5 s (`GooseVoice.BrainDeadline`) before the bank line plays:

| Beat | When | What happens locally |
|---|---|---|
| intro | it lands and glares | first words, pre-voiced during the steal beat ("HERE COMES KEVIN.") |
| taunt10 | 10 s survived | a taunt |
| yell | you shout at the phone | it flinches (RUDE.), backs off, then dashes back at you; it answers what you actually said |
| name | you shout its NAME ("KEVIN!") | on-device speech (Apple, offline) hears the name: it freezes, snaps its head to you, one dramatic honk, "...WHAT.", then a dash. "SORRY" / "PLEASE" / "STOP" get "APOLOGY NOT ACCEPTED." |
| bread | you tap the bread button (once per round) | a roll flies past the goose, it detours and eats it (OM NOM.), comes back angrier |
| dodge | a lunge missed and you moved >= 0.35 m | 80 ms hit-stop, DODGED!, feathers; DODGES on the HUD and the card |
| rage | 30 s | RAGE MODE + a line |
| caught | the tackle | gloating over the results card |
| outlasted | 45 s survived (`outlastSeconds`) | THE GOOSE HAS GIVEN UP: it flops and sulks, the win card, a sore-loser line |

The goose has a name and a title (KEVIN, DESTROYER OF BREAKFAST; the brain invents new ones) and holds a grudge: rounds it
won are counted across launches (`GoosePersona`, mirrored in PlayerPrefs), round 2 opens with "THIS MEANS WAR." and the
results card says STILL MAD. / VERY MAD. Escalation is split in two: **tier** (time: dashes, lunges, rage) and **anger**
(tier + what you did to it: animation, honks, feathers). Bread and shouts raise anger and grant the goose one immediate
dash; they never move the lunge schedule, so the demo stays winnable.

The bread button is a round dark-glass control at the bottom right showing the real slice of toast (the same extruded mesh the
player throws, rendered by a tiny extra camera on the `UIBread` layer into a transparent texture, slowly turning). It sits
quiet while the goose is far and grows with a soft yolk ring pulse as the goose closes in: a save-me, not a nag. One throw
per round (`GooseGameManager.breadPerRound`).


## One-time setup

The Editor menu **Goose Brawl > Setup Project** does everything. It also runs automatically when
`GooseBrawlAutoSetup.SetupVersion` changes (see `GooseBrawlSetup.cs`). It:

1. Adds the `AREnvironment` and `Interactable` layers.
2. Configures iOS player settings (product name GOOSED, home-screen label GOOSED. via the post-build step, camera usage description, portrait, IL2CPP/ARM64, iOS 16+, bundle id).
3. Enables the ARKit loader in XR Plug-in Management and marks ARKit as required.
4. Adds `ARBackgroundRendererFeature` to every URP renderer (needed for the camera feed).
5. Configures the render pipeline assets: render scale 1, MSAA 2x, soft shadows at 1024 / 8 m, depth texture, transparent shadow
   receive, and creates `Assets/Settings/GoosedPostFX.asset`.
6. Sets audio import settings (honks as PCM, the flock recording streamed).
7. Imports TextMeshPro essential resources (UI falls back to legacy Text if that fails).
8. Creates the materials (re-applying every property, so tuning in `GooseBrawlMaterials.cs` always takes effect), bakes the goose
   occlusion map, generates the app icon and launch screen, imports the goose FBX as a Generic rig with looping clips, builds `Goose.prefab`.
9. Builds `Assets/Scenes/GooseBrawl.unity` (AR Session, XR Origin + AR camera with post-processing, plane/raycast/mesh/occlusion/
   environment-probe managers, post-FX Volume, game services, UI, key light).
10. Adds the scene to Build Settings, runs **Goose Brawl > Validate Project**, and switches the build target to iOS.

**Goose Brawl > Validate Project** prints a PASS/WARN/FAIL report in the Console.

### Why the post-FX profile has every effect switched on

URP strips Uber shader variants from the build for every post-processing effect that is not *active* in some
`VolumeProfile` **asset**. `CinematicLookController` drives lens distortion, chromatic aberration, vignette and colour at
runtime on the auto-cloned profile instance, so the asset keeps small non-zero baselines to keep the variants alive.
Do not zero them and do not add Tonemapping (the camera feed is already display-referred; it would grade the real world).

## Testing in the Editor (mock AR)

Press Play in `GooseBrawl.unity`. In the Editor `GooseGameManager.useMockARInEditor` (USE_MOCK_AR) is on,
so a mock room (floor, two walls, a couch, a table, a pillar on the `AREnvironment` layer) and a
first-person mock camera replace AR Foundation.

| Key | Action |
| --- | --- |
| W A S D / arrows | move |
| Q / E | turn |
| right mouse drag | look |
| left click | tap (place nest, tap egg, skip the steal beat on repeat rounds) |
| Space | Start / Steal egg / Resume |
| R | Run again (on game over) |
| B | Throw bread (during the chase, once per round) |
| Y | Simulate a shout (the Editor does not listen on the Mac microphone unless `YellDetector.listenInEditor`) |
| N | Simulate shouting the goose's name (the speech reaction) |
| M | Simulate shouting "sorry" |
| P | On the results card: photo mode; in photo mode: the shutter (PNG lands in `Library/ShareShots/`) |

The mock exercises the whole loop: scanning, placement, egg theft, fly-in, glare, chase, flap-dashes, obstacle
avoidance, honks, haptics (counted), scoring, lunge and game over. `GooseBrawlRemote` (Editor only) polls
`Library/GooseBrawlCommand.txt` for `play`, `smoke` (offline, deterministic), `smokeonline` (talks to `wrangler dev` on
localhost:8787 or the deployed brain), `bench` (the render benchmark), `icon` (renders app-icon candidates from the real goose
to `Library/IconShots/`), `stop`, `setup`, `validate`, `save`, `build`, `settings`, `sentry`, `export`, `layers`, `pipeline`, `audio`;
the smoke test also saves screenshots of every beat to `Library/SmokeShots/`. With `wrangler dev` running, plain `play` also
uses the local brain (the client probes localhost:8787/health in the Editor).

## Sponsor integrations (one architecture, five tracks)

Per-sponsor architecture documents with judges' FAQs: [docs/sponsors/](docs/sponsors/README.md)
(Cloudflare, ElevenLabs, Sentry, OpenAI, Elastic).

```
 iPhone (Unity)                              Cloudflare Worker "goose-brain"                  Third parties
 GooseBrainClient  -- POST /session -->      GooseBrain extends Agent (Durable Object, SQLite)  OpenAI Responses (gpt-5.6-luna):
   sentry-trace + baggage headers              memory: name, title, grudge, rounds, best/last     names the goose, writes every line
                   <-- {name, intro} --        time, last shout, breads, dodges, event log       OpenAI Transcriptions (gpt-transcribe):
                   -- POST /event  -->        workflow: remember -> write -> voice -> cache       what did you shout?
 GooseVoice (3D voice source, beak bob,       GET /audio/<hash> (R2, immutable)                  ElevenLabs TTS (eleven_v3_conversational,
   subtitle pill, honk suppression)           GET /health (Sentry Uptime target)                   wav_22050, a designed goose voice)
 YellDetector (mic loudness, local flinch)    @sentry/cloudflare: errors, tracing continued
 PerfProbe / PerfBenchmark -> Sentry Unity      from the phone, logs, gen_ai spans (AI agent monitoring)
 PerfProbe / PerfBenchmark -> Sentry Unity      from the phone, logs, gen_ai spans (AI agent monitoring)
 GooseTelemetryStream -- POST /telemetry -->  bulk -> Elasticsearch (5 Hz AR sensor stream)      Elastic Cloud Serverless: goosed-shouts
   (5 Hz pose/speed/fps, every 3 s)           session: ES|QL case file; yell: hybrid shout search    (Jina semantic_text), -lines, -runs,
                                              Agent Builder agent in the background; Workflow acts   -telemetry (TSDS); Agent Builder; Workflows
```

- **Cloudflare (Best Agent with a Brain)**: `backend/goose-brain` is a Cloudflare Agent per device: Durable Object state is the
  memory (it survives app launches), OpenAI/ElevenLabs/R2 are its tools, and every game event runs the workflow
  remember -> write -> voice -> cache inside a 4.2 s budget. Workers is the runtime; nothing else runs server-side.
  The same Worker hosts the **Goose Board** (`/board`, see [BOOTH.md](BOOTH.md)): a second Agent (one global Durable Object)
  keeps today's runs, and the page shows the leaderboard next to the *live memory* of whichever phone is playing.
- **OpenAI**: the brain writes every spoken line in character from the memory + event (structured JSON output, content guard),
  invents the goose's name and title, and transcribes the 2-second shout so the goose replies to what you said.
- **ElevenLabs**: every persona owns a voice for life. The male-named geese (KEVIN, GARY, DR. HONK, ...) speak with the stock
  "Callum, Husky Trickster" voice (`N2lVS1w4EtoT3dr4eOWO`), the female-named ones (BRENDA, MARGARET, AGNES, PAMELA) with
  "Laura, Enthusiast, Quirky Attitude" (`FGY2WhTYpPnrIDTdsKH5`), both on the expressive `eleven_v3_conversational` model (audio
  tags like [sighs] and [laughs]) with the "theatrical" delivery picked in `scripts/voice-lab.ts` (sentence case so v3 does not
  shout every line, stability 0.2, style 0.7, speed 1.1; `ELEVENLABS_*` vars in `wrangler.jsonc`, part of the cache key). On the
  phone the voice source adds the goose character (`AudioManager.voicePitch` 1.1, `voiceHighPassHz` 220, `voiceDistortion` 0.1,
  tunable live in the Inspector). The voice is stored with the goose's memory on the Worker and mirrored on the phone, so the same
  goose never changes voice. The same pipeline pre-generated the offline bank (`npm run pregen`: 84 lines x 2 voices under
  `Resources/GooseVoice/m` and `/f`, about 2.8k of the free tier's 10k monthly characters). Voice Design needs a paid plan;
  `npm run design-voice` is there for when that changes.
- **Sentry**: the instrument we benchmark, profile and tune the AR game with (next section), plus the Worker side.
- **Sentry**: the instrument we benchmark, profile and tune the AR game with (next section), plus the Worker side.
- **Elastic (Find the Signal)**: Elasticsearch is the goose's context layer. The messy data is real: multilingual shouted
  speech (Whisper transcripts, `semantic_text` on Jina embeddings), a 5 Hz AR sensor stream from the phone (`goosed-telemetry`,
  a time-series data stream: player/goose position, speed, distance, tier, frame time, thermal state) and every line and round.
  At session start the brain runs seven ES|QL queries in parallel (crowd stats, this player's history and movement profile,
  recent shouts, mercy flag) into a case file the writer taunts from; on a yell it runs one hybrid query (BM25 + Jina dense
  vectors, RRF, optional rerank) to find who else shouted something like that, in any language. An Agent Builder agent
  (`goosed-intel`, five tools) writes deeper notes in the background, and an Elastic Workflow closes the loop on its own every
  five minutes: frame-rate collapses near the goose become Sentry issues, players who keep losing fast get a mercy flag the
  goose reads. Nothing waits on Elastic on the phone: telemetry is batched and answered 202 before indexing; the beats read a
  cached case file. See [docs/sponsors/ELASTIC.md](docs/sponsors/ELASTIC.md).

## Sentry: benchmark, profile, monitor (not just errors)

Sentry has no Profiling or Session Replay for Unity, so the phone-side depth comes from **Tracing with frame-level measurements**
and **structured Logs** produced by the game's own harness, `PerfProbe`:

- Every frame: wall-clock frame time, `FrameTimingManager` CPU main/render thread and GPU time, memory, GC per frame, draw calls,
  plus the context that explains a bad frame (goose state/tier, live particles, LiDAR mesh chunks, planes, tracking, occlusion
  mode, MSAA, shadow resolution, post-FX, a brain request or WAV decode in flight, the mic, iOS thermal state).
- A round is a transaction `game.round` with measurements (`frame.p50_ms`, `frame.p95_ms`, `frame.max_ms`, `frames.over_33ms`,
  `gpu.p95_ms`, `mainthread.p95_ms`, `mem.peak_mb`, `gc.kb_per_s`, `draw_calls.p95`, `mesh.chunks_max`, `particles.max`,
  `thermal.max`) and one span per phase (`fly_in`, `glare`, `chase.tier0..3`, `dash`, `lunge`, `eating`, `flinch`) and per
  operation (`brain.session`, `brain.event.<beat>`, `voice.fetch`, `voice.decode`, `voice.play`, `yell.detect`, `bread.spawn`).
  The `sentry-trace` header continues the trace into the Worker, so one trace runs phone -> Worker -> OpenAI -> ElevenLabs.
- **Metrics** (Sentry Application Metrics): every round emits `frame.p95_ms`, `frame.p50_ms`, `gpu.p95_ms`, `mem.peak_mb`,
  `frames.spikes`, `quality.tier`, `round.seconds` and `rounds` (tagged by outcome / config / device), the brain client emits
  `brain.request_ms` per call, the voice emits `voice.spoken` / `voice.fallback`, the yell detector `yell.trigger` / `yell.gated`,
  speech `speech.recognized` (tag `matched` = name / apology / none), the results card `share.opened` / `photo.taken`, the board `board.posted` (log).
- Any frame over 33 ms on the device is a structured log `frame.spike` with all of that context. Everything the yell detector
  decides is logged with its levels (`yell.trigger`, `yell.gated reason=honk`), as are bread, dodges, fallbacks and quality changes.
- **Adaptive quality**: when the rolling p95 frame time stays above 20 ms the ladder steps down (MSAA 2x -> occlusion temporal
  smoothing off -> grain/bloom/chromatic aberration off), never touching the goose, shadows or the shadow catcher, and logs
  `quality.tier_changed` with the thermal state.
- **Benchmark**: hold three fingers on the title screen for 1.5 s, launch the app with `GOOSE_BENCH=1` from the Mac
  (`scratchpad/bench_device.sh`), or `bench` in the Editor. The game drives itself to a chase the goose cannot win and runs
  ten render configurations for 15 s each (`baseline`, `msaa4x`, `msaa_off`, `no_occlusion_smoothing`, `no_occlusion`,
  `no_postfx`, `shadows2048`, `mesh_on`, `render_scale_085`, `baseline_end`), each a `bench.<config>` transaction with the same
  measurements plus a `bench.result` log, and writes a JSON report into the app's Documents folder. The process, the decision
  rules and the results live in [docs/PERFORMANCE_BENCHMARK.md](docs/PERFORMANCE_BENCHMARK.md).

Setup: put the Unity project's DSN in `./sentry.dsn` (gitignored) and run **Goose Brawl > Configure Sentry** (writes
`Assets/Resources/Sentry/SentryOptions.asset`: tracing 100%, logs on, iOS native on, no symbol upload so the Xcode build never
needs network). The Worker's DSN goes into `backend/goose-brain/wrangler.jsonc` (`SENTRY_DSN`). Create an Uptime monitor on
`https://goose-brain.<account>.workers.dev/health` in the Sentry UI. Environments: `editor-mock` (smoke runs) and `device`.

**What Sentry told us**: see the "What the harness already changed" and "Results" sections of
[docs/PERFORMANCE_BENCHMARK.md](docs/PERFORMANCE_BENCHMARK.md) (LiDAR mesh colliders and environment probes off, HUD rebuilds
throttled, MSAA 2x / 1024 shadows kept, the per-config p95 table from the iPhone 15 Pro).

## Getting the brain online (once)

```bash
cd backend/goose-brain && npm install
npx wrangler login
npx wrangler r2 bucket create goose-audio
npx wrangler secret put OPENAI_API_KEY
npx wrangler secret put ELEVENLABS_API_KEY
cp .dev.vars.example .dev.vars          # same two keys for the scripts / local dev
npm run design-voice                     # prints ELEVENLABS_VOICE_ID -> wrangler.jsonc vars
npm run pregen                           # real voice for the offline bank (Assets/Resources/GooseVoice)
npx wrangler deploy                      # -> https://goose-brain.<account>.workers.dev
npm run pregen -- --force                # after a tone change: voice the bank locally (2 voices x 84 lines ~ 2.8k characters)
npx tsx scripts/upload-bank.ts           # push it into the Worker's cache for free (same keys)
curl -X POST https://goose-brain.<account>.workers.dev/warm   # or let the Worker voice what is missing (repeat until done:true)
```

Paste the Worker URL into `Assets/Resources/GooseBrainUrl.txt` and run **Goose Brawl > Validate Project**. Without any of this the
game still plays from the offline bank.

Current deployment: `https://goose-brain.mohammad-rashid7337.workers.dev` (R2 bucket `goose-audio`, ElevenLabs secret set, no
OpenAI key by choice: lines come from the 84-line script, voiced live and cached). Sentry is enabled in Unity and on the Worker
with the same project DSN; the Uptime monitor on `/health` still has to be created in the Sentry UI.

## Building for iPhone

1. **Goose Brawl > Build iOS Xcode Project (Release)** builds `Builds/iOS/Unity-iPhone.xcodeproj` (release: no development
   console overlay, no profiler hooks; the perf harness still works because frame timing stats are enabled)
   (or use File > Build Profiles > iOS; Setup already switched the target and added the scene).
2. Player Settings that matter are pre-set: camera usage description, portrait, Metal, IL2CPP, ARM64,
   minimum iOS 16.0, bundle id `com.goosebrawl.eggsnatcher` (change it to your team's if needed).
3. Open the Xcode project, set your Team under Signing & Capabilities, plug in the iPhone and Run. From the
   command line (automatic signing, replace the team id and device UDID):

```bash
xcodebuild -project Builds/iOS/Unity-iPhone.xcodeproj -scheme Unity-iPhone -configuration Release \
  -destination "platform=iOS,id=<UDID>" -derivedDataPath Builds/DerivedData \
  -allowProvisioningUpdates -allowProvisioningDeviceRegistration DEVELOPMENT_TEAM=<TEAMID> CODE_SIGN_STYLE=Automatic build
xcrun devicectl device install app --device <CoreDevice-id> "Builds/DerivedData/Build/Products/Release-iphoneos/GOOSED.app"
xcrun devicectl device process launch --device <CoreDevice-id> --console com.goosebrawl.eggsnatcher
```

4. On first launch accept the camera permission.

See KNOWN_LIMITATIONS.md for what was and was not verified on the device.

## Look, sound, feel

- **Rendering**: MSAA 2x at full render scale (alpha-to-coverage on the goose's feather cards; 2x is the phone budget, the
  benchmark compares 4x), URP post-processing (bloom on highlights, dark vignette, gentle grade, film grain; no tonemapping),
  real soft shadows (1024 map, 8 m) from the goose, egg and nest onto an invisible **shadow catcher** on your floor (`GooseBrawl/ShadowCatcher`: ZTest off, virtual occluders via the
  depth prepass, real occluders via the ARKit environment depth with a soft tolerance so LiDAR noise never speckles), a
  multiplicative contact patch under the goose, ARKit **light estimation** driving the key light and ambient (lumens +
  colour temperature; `ARLightRig`), ARKit **environment probes** for real reflections on the egg and the goose's beak and eyes.
- **Goose material**: 2048 albedo, full-res normal, metallic/smoothness, baked occlusion (from the packed ORM map), double
  sided on the shared asset; a grey variant shows up on roughly one round in three.
- **Props**: lathe egg with a speckled shell; nest = lumpy lathe bowl + one combined mesh of ~80 woven strands with a generated
  normal map + down feathers + a warm point light while the egg is home; the cracked egg = two jagged shell halves, six shards,
  a yolk dome and a spreading white. Feathers flutter with a noise field; dust puffs on footsteps and landings.
- **State grade** (`CinematicLookController`): danger darkens and reddens the vignette; impacts punch the lens and add
  chromatic aberration; slow motion desaturates; rage desaturates slightly. No red UI paint, only a subtle HUD jitter.
- **UI**: title and results in the cream / yolk / brown brand; everything over the live camera is dark glass with light
  condensed type. The **locator** pins a goose icon, an arrow and the distance to the screen edge whenever the goose is
  off-screen (BEHIND YOU at the bottom when it is behind the phone) and pulses on every honk. Comic HONK bubbles pop only
  while the goose is on screen. Pause (button or when
  the app goes to the background), sound and haptics toggles, MOVE NEST from the results screen.
- **Audio**: the honks are real geese (sixteen slices, shuffle-bag so nothing repeats). Everything else is synthesized at
  44.1 kHz in round-robin variants: webbed footsteps, wing flaps and a wing-beat loop for the fly-in, whooshes, the flap-dash
  push-off, the tackle (thud + feathers + beak clacks), the egg crack with shell scatter, shell tap, twigs settling, button
  taps, a heartbeat that is audible on a phone speaker. (The synthesized breathing loop is off: once the goose could talk it
  read as a random squeaky creature; `ChaosAudioController.breathingEnabled`.) iOS audio session: Playback whenever the
  microphone is not recording, so the game is audible with the ringer switch off. Goose sources use a custom rolloff and a **low-pass that
  muffles the goose when it is behind you**; a mild room reverb zone sits at the nest; slow motion drops pitch and cutoff.
  The distant flock joins in rage mode. There is deliberately no music.
- **Haptics** (`HapticsService` + `GooseHaptics.mm`, Core Haptics): the heartbeat pattern fires on the same tick as the
  heartbeat sound with intensity following danger; footsteps are felt within 3 m; honks buzz when close; wing beats during the
  fly-in; lunge wind-up ramp, launch, landing; the tackle; the egg crack; rage rumble; success on a new best. Two rate-limit
  channels keep ambient ticks from swallowing events.

## Scoring

Survival **time** is the score. The HUD shows the timer big, plus honks survived and your best time. The game-over card shows
the time big, honks survived and the best time. Best time and games played are the only persistent data (PlayerPrefs, via `ScoreManager`).

## Results card, photo, Goose Board

- **Run recap**: under the big time the card lists what happened, only the non-zero items: `DASHES SURVIVED n · LUNGES SURVIVED n
  · BREAD n · RAGE REACHED` (`RoundRecap`, built by `GooseGameManager.BuildRecap`).
- **The photo** (`GooseShareService`): at the tackle the game grabs the slow-motion frame with the HUD hidden and a cream
  GOOSED. stamp (goose name + title, `GOT YOU AT 0:32.4`, `HACK THE NORTH 2026`); a win captures the sulking goose (`OUTLASTED IN`).
  **SHARE** opens the iOS share sheet (`Assets/Plugins/iOS/GooseShare.mm`: AirDrop, Messages, Save Image). **PHOTO WITH KEVIN**
  hides the card and switches to the front camera (ARKit Face Tracking must be included in the build; the validator checks this). The shutter waits for two frames from the requested camera. The goose rides the phone over your shoulder (beside the line of sight,
  head at lens height, upright), stares down the lens with its head cocked and every couple of seconds honks at it, throws
  its wings up or pecks at the camera. **FLIP** goes back to the world camera with the goose posing where it stood (turns
  to follow you, flaps and honks); the shutter captures and shares either. The camera switch needs a `ConfigurationChooser`
  that puts the user-facing camera first (`GooseConfigurationChooser` in ARBootstrapper.cs): AR Foundation's default picks
  the ARKit configuration with the most requested features, so planes + meshing + occlusion always outvoted the selfie
  request and the rear camera stayed. Planes, meshing and environment occlusion are off for the selfie (people occlusion requested only when supported by the front-camera configuration,
  otherwise the goose is composed beside your face) and restored on BACK. In the Editor every capture is written to
  `Library/ShareShots/`.
- **Goose Board**: when the Worker is reachable the card shows a name field and **POST TO BOARD** ("#3 ON THE BOARD"); the
  title screen shows today's top three. Everything degrades silently offline (row and ticker hidden, card shorter).
- **It knows its name**: the 2 s shout clip the mic already captures is also run through Apple's on-device recogniser
  (`Assets/Plugins/iOS/GooseSpeech.mm`, `SpeechRecognizer.cs`); the flinch holds until the transcript lands (cap 1.6 s) and the
  bank reply plays only when the shout was neither its name nor an apology. Silently off when the phone has no on-device model.

## Tuning

All the interesting numbers are serialized fields:

- `GooseChaseController`: walk/run/max speed (gentle by design), time to max speed, walk phase, catch distance, lunge trigger
  distance, replan rate, honk intervals, angry flap interval, think pauses; **Entrance**: fly-in distance/height/speed, glare seen
  duration, glare max duration, grace period; **Escalation**: tier start times (8/18/30 s), dash cooldown per tier, dash
  min/max/stop distance, rage speed/honk multipliers.
- `GooseAttackController`: lunge height/duration/cooldown, catch radius, minimum chase time before a lunge (dash values live in `HopSpec.Dash`).
- `GooseObstacleAvoidance`: body radius/height, look-ahead, emergency stop distance, scoring weights.
- `GooseGameManager`: landing distances, danger distance, repeat-round beat speed.
- `CinematicLookController`: vignette / saturation baselines and danger maximums. `ARLightRig`: neutral lumens, min/max scale.
- `ChaosAudioController`: heart rate range and start danger, flock volume (breathing off by default). `AudioManager`: master volume, goose min/max distance.
- `HapticsService`: channel rate limits. `GooseBrawlMaterials.cs`: every material property. `GoosePrefabBuilder.TargetHeightMeters` (1.05 m).

## How the goose avoids walls

- On LiDAR iPhones (15 Pro) `ARMeshManager` produces scene-reconstruction chunks (density 0.3, no normals, two in
  flight). The mesh prefab has a `MeshCollider` and no renderer, so AR Foundation bakes the collision data off the
  main thread; `AREnvironmentMeshController` keeps every chunk on the `AREnvironment` layer. This is what makes a
  chair or a table exist for the goose.
- Detected planes (floor and vertical walls) also carry `MeshCollider`s as a fallback when meshing is unavailable.
- The goose body is a full-height capsule (feet to head, `GooseObstacleAvoidance.bodyHeight`): spawn and landing spots
  need it clear, with scanned floor under it and a line of sight from the phone. A kinematic trigger copy of that
  capsule (Ignore Raycast layer, no contacts) lets `Physics.ComputePenetration` push a standing goose out of geometry
  that turns up around it later (the chair chunk that arrives after the landing); when the way out is vertical it hops
  to the nearest free spot. The camera bubble (`GooseMovement.minCameraDistance`) holds in every grounded state.
- 4-8 times per second `GooseObstacleAvoidance` capsule-casts the goose body along 0, +/-30, +/-60, +/-90 degrees
  (wider when stuck), checks for floor under each candidate and scores clearance, deviation, heading continuity and
  progress toward the player. Blocked directions slow the goose; if nothing is clear it stops, honks, backs up and turns.
  Flap-dashes are only attempted when the whole hop path is clear and there is floor at the landing spot.
- With no environment data yet, the goose moves a little slower and stops at anything it does hit.

# Copy to .dev.vars (gitignored). Same names as `wrangler secret put`.
OPENAI_API_KEY=sk-...
ELEVENLABS_API_KEY=...
# Optional overrides for local dev
# ELEVENLABS_VOICE_ID=...
# MOCK_AI=1
# Elastic Cloud project API key (Kibana > API keys). The endpoints are in wrangler.jsonc.
ELASTIC_API_KEY=
